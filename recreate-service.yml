apiVersion: v1
kind: Service
metadata:
  name: textutils-service
  namespace: recreate-namespace
spec:
  selector:
    app: textutils    
  type:  NodePort
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30000
