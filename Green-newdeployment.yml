apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: textutils-green
  name: textutils-green
  namespace: bluegreen-namespace
spec:
  replicas: 2
  selector:
    matchLabels:
      app: textutils-green
  template:
    metadata:
      labels:
        app: textutils-green
    spec:
      containers:
      - name: textutils-green
        image: omsantoshharsule/textutils-newdeploy:latest
        ports:
        - containerPort: 5000
        resources:
          requests:
            memory: "64Mi"
            cpu: "250m"  
          limits:   
            memory: "128Mi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: textutils-service
  namespace: bluegreen-namespace
spec:
  selector:
    app: textutils-green
  type: NodePort
  ports:
  - port: 5001
    targetPort: 5000
    nodePort: 30001