apiVersion: v1
kind: Service
metadata:
  name: three-tier-service
  namespace: recreate-namespace
spec:
  selector:
    app: three-tier   
  type:  NodePort
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30000
